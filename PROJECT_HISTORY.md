# Sanremo Russia — история проекта

Файл ведётся **только добавлением новых записей в конец**. Старые записи не переписывать; если прежняя запись оказалась неточной, добавить новую запись с исправлением, источником и датой.

## 2026-08-25 — исходная сборка и первая переработка

- Создан первый B2B-сайт Sanremo Russia (`0e3e5f3`), затем интерфейс пересобран по подробному ТЗ (`2b7959a`).
- Подключены локальные шрифты и уточнён логотип (`3dc1f2e`); далее логотип несколько раз заменялся и переводился на более резкий/векторный файл (`1bc2e6a`, `3bc99e8`, `80498e0`).
- Подключены каталог BIO и изображения портала (`b62076f`), затем уточнены каталог, запчасти, контакты, медиа и наличие (`5ed8933`, `2302f7a`, `e787555`).
- Первая публикационная история объединена с локальной (`1227399`, `e7c2595`).
- После отрицательной оценки ранней визуальной версии восстановлен запрошенный светлый прототип и его runtime (`c1060cf`, `552c991`, `12f9ad6`, `374413f`).
- Импортирован дилерский справочник, добавлен поиск территории, упрощены фильтры и вместо неподтверждённой карты создана сводка покрытия (`7056b33`, `5a0f697`, `7a7a45f`).
- Унифицированы состояния выбора и CTA (`b75772c`, `cf3394f`, `d8fb5d2`).

Источник: Git-история; пользовательские сообщения и скриншоты 2026-08-25. Проверено 2026-08-31.

## 2026-08-26 — карточки, D8, кейсы и визуальная система

- Блок кейсов сделан целиком кликабельным, добавлены новости и технологии (`9e10ead`, `f1f0276`).
- Добавлены D8 и D8 PRO, затем заменены их изображения на версии со светлым фоном (`ceab9f4`, `b1c827b`).
- Переработан селектор конфигураций, компактнее показано наличие, исправлен сброс прокрутки (`bc80243`, `51ca7d5`, `5b6b290`).
- Построена референсная страница ZOE 2026 и последовательно доработаны её первый экран, галерея, миниатюры и фото (`f2849aa`—`a35ce36`).
- Добавлены и уточнены российские кейсы, переработаны галереи и редакционные продуктовые истории (`61c0853`—`8836f3f`).
- Принята типографическая система с Playfair Display (`a6b72ee`), упрощён футер производителя (`3bf115a`).
- Проект подготовлен к статическому Timeweb deployment (`2240f4f`); обновлена фотография главной (`4d458b2`).

Источник: Git-история. Проверено 2026-08-31.

## 2026-08-27—2026-08-30 — контент, SEO, дилеры и главная

- Обновлены российские кейсы кофеен (`b9ed3df`).
- Опубликован очищенный SEO-контент по разделам (`8bcf142`).
- Обновлена дилерская сеть и восстановлена компоновка «Где купить» (`0ea2d9b`, `23a656a`).
- Восстановлена структура главной страницы (`d09cd8a`).

Источник: Git-история. Проверено 2026-08-31.

## 2026-08-31 — продуктовые правки до подключения Timeweb

- Убран повторный логотип Sanremo на странице «О компании» (`458c573`).
- Подборщик переведён на последовательное сужение полного списка моделей (`47b0efb`).
- Восстановлен полный дилерский справочник (`96543d3`).
- Добавлено сохранение выбора с главной в подборщике (`ad36fd9`).
- Из галереи YOU удалено неоднозначное белое/стальное изображение (`1c50025`).
- YOU перепозиционирована как компактная high-end машина для specialty и требовательного домашнего использования (`851455a`).
- На страницах моделей блок связанных моделей заменён на «Весь модельный ряд Sanremo» с шестью альтернативами (`6c87093`).
- Проект подготовлен к Timeweb (`5596933`).

Источник: Git-история и пользовательские сообщения 2026-08-31. Проверено 2026-08-31.

## 2026-08-31 — GitHub → Timeweb и исправления staging

- Создана отдельная ветка deployment и объединена с основной историей (`13b10b3`).
- Исправлена конфигурация Timeweb (`8493ea6`). Публикация подтверждена на `https://staging.sanremomachines.ru/`.
- Подборщик начал показывать модели до завершения всех шести ответов; с главной убрана лишняя повторяющаяся маркировка (`d62c938`).
- Выполнен служебный запуск повторного deployment (`22d7b7e`).
- Исправлена передача выбранного формата/объёма с главной в основной Next.js-подборщик (`22935be`).
- Восстановлены пропавшие фото главной, новостей, кейсов и D8: файлы перекодированы, получили новые URL и были проверены как реальные JPEG на staging (`d8acc5d`).
- Исправлена ссылка из iframe-главной: вместо относительного локального `/choose/` используется абсолютный staging-адрес, поэтому браузер больше не открывает `Файл /choose` (`d8acc5d`).
- YOU добавлена в сценарий Specialty; опубликованная запись YOU на `/choose/?format=specialty` проверена после deployment (`b5b9d8e`).

Источник: Git-история, HTTP-проверки staging и пользовательские скриншоты 2026-08-31. Проверено 2026-08-31.

## 2026-08-31 — фиксация состояния

- Созданы `PROJECT_STATE.md`, `PROJECT_HISTORY.md` и `AGENTS.md`.
- Зафиксировано, что фактический источник staging — `biotempbio/Sanremo_Site`, тогда как постоянная локальная `main` и старый `DEPLOY.md` всё ещё ориентированы на `biotempbio/sanremo-russia-site`.
- Зафиксирован риск временного deployment-worktree и необходимость безопасной устойчивой локальной копии актуальной ветки.

Источник: Git/remotes/worktree, HTTP и DNS-проверки 2026-08-31.
